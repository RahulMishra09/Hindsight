name: Publish Artefacts

# Triggered on version tags: v0.3.0 (dataset, Week 3), v0.5.0 (model, Week 5)
# This workflow is a STUB — steps are commented out until their respective weeks.
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  publish-dataset:
    name: Publish Dataset to Hugging Face Hub
    runs-on: ubuntu-latest
    # Enable in Week 3 when the labelled dataset is ready
    if: false  # Remove this line in Week 3
    steps:
      - uses: actions/checkout@v4

      # - name: Install uv
      #   uses: astral-sh/setup-uv@v5
      #   with:
      #     version: "latest"

      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.12"

      # - name: Install dependencies
      #   run: uv sync

      # - name: Push dataset to Hugging Face Hub
      #   env:
      #     HF_TOKEN: ${{ secrets.HF_TOKEN }}
      #     HF_DATASET_REPO: ${{ vars.HF_DATASET_REPO }}
      #   run: |
      #     uv run python scripts/push_dataset_to_hub.py \
      #       --repo "$HF_DATASET_REPO" \
      #       --tag "${{ github.ref_name }}"

  publish-model:
    name: Publish ONNX Model to Hugging Face Hub
    runs-on: ubuntu-latest
    # Enable in Week 5 when the ONNX model is exported and validated
    if: false  # Remove this line in Week 5
    steps:
      - uses: actions/checkout@v4

      # - name: Install uv
      #   uses: astral-sh/setup-uv@v5
      #   with:
      #     version: "latest"

      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.12"

      # - name: Install dependencies
      #   run: uv sync

      # - name: Export model to ONNX int8
      #   run: |
      #     uv run python scripts/export_onnx.py \
      #       --checkpoint models/deberta-v3-base-incidents \
      #       --output models/deberta-v3-base-incidents-int8.onnx

      # - name: Validate ONNX model
      #   run: uv run python scripts/validate_onnx.py

      # - name: Push model to Hugging Face Hub
      #   env:
      #     HF_TOKEN: ${{ secrets.HF_TOKEN }}
      #     HF_MODEL_REPO: ${{ vars.HF_MODEL_REPO }}
      #   run: |
      #     uv run python scripts/push_model_to_hub.py \
      #       --repo "$HF_MODEL_REPO" \
      #       --onnx-path models/deberta-v3-base-incidents-int8.onnx \
      #       --tag "${{ github.ref_name }}"

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: []  # Add publish-dataset and publish-model when enabled
    # Enable in Week 6 for the full system release
    if: false  # Remove this line in Week 6
    steps:
      - uses: actions/checkout@v4

      # - name: Create GitHub Release
      #   uses: softprops/action-gh-release@v2
      #   with:
      #     generate_release_notes: true
      #     files: |
      #       models/*.onnx
